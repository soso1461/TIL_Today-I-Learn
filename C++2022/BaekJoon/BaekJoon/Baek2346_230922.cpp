#include <iostream>
#include <deque>
#include <cmath>

using namespace std;

int N;
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int num;
	cin >> N;
	num = floor(N / 2);

	int arr[2001] = {0};
	deque<int>d;
	for (int i = 0; i < N; i++) {
		cin >> arr[i];

		arr[num];
	}



}