#include <iostream>
#include <vector>

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	vector<vector<int>> graph(4, vector<int>(4));

	for (int i = 0; i < 4; i++) {
		for (int j = 0; j < 4; j++) {
			cin >> graph[i][j];
		}
	}

	int x1, y1, x2, y2;
	cin >> x1 >> y1 >> x2 >> y2;

	vector<
	
}